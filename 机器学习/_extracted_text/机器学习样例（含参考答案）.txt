

===== Page 1 =====
《机器学习》样例(含参考答案)
一、判断题（10 小题，每题 2 分，共 20 分）
1. 机器学习问题的基本假设是：训练样例和测试样例来自于以同一个总体且相互独
立。（T）
2. 机器学习的训练目标是尽量减少模型在训练集上的误差。（F）
3. 任何具有泛化能力的学习器都是有偏的。（T）
4. 当训练数据较少、模型过于复杂时，训练过程更容易发生过拟合。（T）
5. 基于实例的学习算法的核心是基于测试样例局部空间的目标逼近而不是全局样例
空间的目标逼近。（T）
二、（20 分）概念学习与候选消除算法。
已知概念学习的目标假设采用实例的各属性约束的合取形式表示，训练集如下所示：
其中包含 4 个训练样例，Example 列为样例编号，每一个训练样例包含<Sky,
AirTemp,Humidity,Wind,Water,Forecast>等 6 个属性特征和 1 个 EnjoySport 决策属
性；
1． （4 分）请指出基于候选消除算法的概念学习的归纳偏执；并说明这种偏执是空
间偏执还是算法偏执？
参考答案：
（1）候选消除算法的归纳偏执：目标假设可以用属性约束的合取表示；
（2）上述偏执为空间偏执。限制了目标假设空间的范围。
2． （6 分）候选消除算法中，“一般边界 G”和“特殊边界 S”的初始值是什么？请
依次列出候选消除算法在处理每一个训练样例（包括正例或负例）后“一般边界
G”和“特殊边界 S”的演化结果；
参考答案：
（1）G 的初始值为<?,?,?,?,?,?>；S 的初始值为<ø,ø,ø,ø,ø,ø,ø>；
（2）
第1页/共4页

===== Page 2 =====
3． （6 分）请列出候选消除算法最后生成的变型空间（Version Space）中的所有假设；
参考答案：
第2页/共4页

===== Page 3 =====
4． （4 分）若给定以下训练集，依据上述的候选消除算法，学习过程是否收敛？为什
么？
参考答案：
（1）算法不收敛；
（2）上述训练集中 sky 属性的取值为 3 个（Sunny，Cloudy，Rainy），与该训练
集保持一致的目标假设为属性约束的合取的析取，不属于候选消除算法的假设空
间（属性约束的合取）；
三、（20 分）感知机分类器。
已知感知机分类超平面定义为：𝑓 (𝑥) ≜ 𝑤 ∙ 𝑥 + 𝑏 = 0，其中：𝜃 = {𝑤, 𝑏}；训练集𝐷 = {<
𝜃
𝑥 , 𝑦 >; 𝑖 = 1 … 𝑁}，其中：𝑥 ∈ 𝑋 ⊆ 𝑅𝑛，𝑦 ∈ {±1}。请回答以下问题：
𝑖 𝑖 𝑖 𝑖
1． （4 分）请给出感知机模型的分类决策函数；并说明如何判断一个样例是否被误
分类？
参考答案：
（1）𝐬𝐢𝐠𝐧(𝒇 (𝐱))
𝜽
（2）若𝒚𝒇 (𝒙) < 𝟎则被误分类；
𝜽
2． （6 分）请写出感知机模型原始形式学习算法的目标函数（即训练集上的经验损
失）；以及该目标函数分别在整体梯度下降（GD）和统计梯度下降（SGD）策略
下的参数迭代公式；
参考答案：
（1）优化目标函数： ，其中 M 为误分类的训练样例集合；
第3页/共4页

===== Page 4 =====
（2）整体梯度下降（GD）和统计梯度下降（SGD）策略下的参数迭代公式：
3． （6 分）请写出感知机模型的对偶形式，包括参数和决策函数；并给出当训练样例
被误分类时，参数学习的迭代公式；
参考答案：
（1）对偶形式的决策函数： ，α 和 b 为参数
（2）误分类时参数学习迭代公式：
4． （4 分）什么是数据集线性可分性？如果数据集线性不可分，上述感知机模型训
练算法是否收敛？
参考答案：
（1）对于给定的数据集合 T，若存在超平面 S 将集合 T 中的正例和负例正确的
划分到 S 的两侧，则称 T 线性可分；
（2）若数据集线性不可分，感知机模型训练算法不收敛。
第4页/共4页